import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Selecionar extends Component {
  
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

Selecionar.propTypes = {

};

export default Selecionar;
