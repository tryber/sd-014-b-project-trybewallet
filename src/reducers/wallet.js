// Esse reducer será responsável por tratar o todas as informações relacionadas as despesas

const INITIAL_STATE = {
  currencies: [],
  expenses: [],
};

export default function reducerWallet(state = INITIAL_STATE, { type }) {
  switch (type) {
  default:
    return state;
  }
}
