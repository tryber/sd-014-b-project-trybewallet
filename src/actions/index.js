// Coloque aqui suas actions
export const LOGIN_OK = 'LOGIN_OK';
export const CURRENCY_OK = 'CURRENCY_OK';

export const statusUser = (payload) => (
  {
    type: LOGIN_OK,
    payload,
  }
);

// async func

export const statusCurrency = (payload) => (
  {
    type: LOGIN_OK,
    payload,
  }
);
